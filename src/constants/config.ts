// Local backend WebSocket URL. `localhost` works for web and
// simulators/emulators, but Expo Go on a physical phone can't reach
// `localhost` of your dev machine — replace it with your machine's LAN IP
// (e.g. `ws://192.168.1.23:8000/ws/chat`) when testing on a real device.
export const CHAT_WS_URL = 'ws://localhost:8000/ws/chat';
