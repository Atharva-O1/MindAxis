// Local backend URLs. `localhost` works for web and simulators/emulators,
// but Expo Go on a physical phone can't reach `localhost` of your dev
// machine — replace both with your machine's LAN IP (e.g. `192.168.1.23`)
// when testing on a real device.
export const API_BASE_URL = 'http://localhost:8000';
export const CHAT_WS_URL = 'ws://localhost:8000/ws/chat';
